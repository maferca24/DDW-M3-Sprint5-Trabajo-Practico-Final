//dashboard deberá mostrar un resumen de los países hispanohablantes, incluyendo su nombre, capital, población y bandera. Además, se pueden incluir gráficos o estadísticas relevantes sobre los países.
<h1>Dashboard de Países Hispanohablantes</h1>
<!-- <div class="dashboard-container">
    <% if (countries && countries.length > 0) { %>
        <table>
            <thead>
                <tr>
                    <th>Bandera</th>
                    <th>Nombre</th>
                    <th>Capital</th>
                    <th>Población</th>
                </tr>
            </thead>
            <tbody>
                <% countries.forEach(country => { %>
                    <tr>
                        <td><img src="<%= country.flag %>" alt="Bandera de <%= country.name %>" width="50"></td>
                        <td><%= country.name %></td>
                        <td><%= country.capital %></td>
                        <td><%= country.population.toLocaleString() %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>

        <!-- Aquí podrías agregar gráficos o estadísticas adicionales -->
    <% } else { %>
        <p>No se encontraron países hispanohablantes.</p>
    <% } %> -->
<div>
    <p>En construcción...</p>
</div>

